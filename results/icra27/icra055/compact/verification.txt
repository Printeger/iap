ICRA-055 / IAP-RQ-423
result: BLOCKED_PRODUCTION_SURFACE_EXCEEDS_EIGHT_OUTPUT_CONTRACT

Hermetic launcher:
- exact root: /home/dev/ws_iap/src/iap/results/icra27/icra055;
- controlled modes: unittest, syntax, flake8, canonical-json only;
- exact task-local HOME, ROS_HOME, ROS_LOG_DIR, TMPDIR and XDG_RUNTIME_DIR;
- XDG mode 0700 and pre-import guard discovery;
- child exit and external mutation are distinct structured results.

External ROS log proof:
- every Python invocation captured relative name, type, mode, uid/gid, size,
  mtime_ns, ctime_ns, symlink target and regular-file SHA-256 before and after;
- formal full-discovery inventories contain 17,759 entries;
- before/after canonical SHA-256 is identical:
  82b029dee5cd6444ee812316381565531308365dd6704cbc2df938a4fa68eee9;
- cmp exit 0 and structured delta [].

Classifier closure:
- both r3 and legacy XDG conditions require exact IfCondition wrapper,
  Equals/NotEquals operator, LaunchConfiguration("experiment") left operand
  and P4_G0C_EXPERIMENT_V3 name right operand;
- nine condition adversaries reject, including wrong key/constant, reversed
  operands, string lookalike, wrong operator/wrapper, extra substitution,
  missing condition and wrong legacy operator;
- exact production action multiset remains FAST DDS, Qt, r3 XDG, legacy XDG;
- module, sync, async and nested scopes are inspected;
- os, shutil, pathlib/Path and subprocess aliases are normalized; unknown
  namespace members, dynamic lookup, modes, flags, targets and output keywords
  reject;
- canonical descendants reject absolute, parent, empty and multi-component
  operands; subprocess positional streams, final/dynamic flags and unknown
  module-qualified mutation calls also reject;
- 32 mutation-family cases and all five subprocess helpers are covered;
- the inventory remains exactly 50 path records and reaches five child
  environment roots, four environment actions and all eight output semantics;
- exact contract validation now rejects the additional `runs_root` semantic
  root. The production runner really writes `p4_g0c_runner_state.json` and
  creates run/environment containers there; these are not exact or canonical
  descendants of any of the eight registered mutable outputs. The runner is
  outside the ICRA-055 allowlist, so truthful classification is BLOCKED.

Independent review remediation:
- the initial Standards review found one blocking command-provenance gap and
  three nonblocking architecture observations; the complete reproducible
  command block now closes the blocker without broad refactoring;
- the initial Spec review found five classifier/test blockers plus the pending
  final handoff: canonical-child proof, dynamic/unknown mutation coverage,
  subprocess positional/final/dynamic flags, invoked top-level guard proof and
  an eight-output assertion that filtered extras. Focused RED/GREEN tests and
  exact production-contract validation close all implementation blockers.
- final Spec re-review found five residual blockers. Canonical sibling,
  nested/dynamic namespace, all-helper positional stream, ExecuteProcess
  dynamic-flag and recursive guard discovery gaps were repaired RED/GREEN.
  The remaining no-extra-root finding exposed the production `runs_root`
  contradiction above and therefore changes the task result to BLOCKED.

Formal commands all used run_p4_g0c_tests.py with the exact ICRA-055 root:
- unittest discover -s test -p 'test_p4_g0c_*.py' -v: 111/111, exit 0;
- unittest launch-contract discovery: 11/11, exit 0;
- unittest launch-golden discovery: 16/16, exit 0;
- syntax: 6/6, exit 0;
- fatal-only flake8: exit 0;
- canonical JSON: 4/4, exit 0;
- unittest discover -s test -p 'test_*.py': 466/466, exit 0;
- git diff --check: exit 0.

No build/install/log product, compiled test, CTest, GPU preflight, ROS process,
launch service, runner/analyzer CLI, calibration, main flow, smoke,
qualification, identity consumption or threshold action ran. No production
launch/runner/science/config/protocol/registry/dependency/lineage byte changed.
ICRA-054 evidence and external incident records remain preserved. The PDF is
unmodified, untracked and unstaged.
