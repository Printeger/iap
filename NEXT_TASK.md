# ICRA-049 — Bind production top-level G0C effective evidence fail-closed

> Active gate: `P4_G0C_REPLACEMENT_EVIDENCE_BINDING_REPAIR`
> Owner: `DEEPSEEK`
> Activation: `TASK_READY`
> Supervisor verdict: `ICRA048_REVIEW_REQUEST_CHANGES_TEST_PLANNER_TOP_LEVEL_BINDING`
> Requirement mapping: `IAP-RQ-423`
> Conference route: conditional P0 -> P4 -> P5
> This task: one bounded synthetic evidence-binding repair; no build, GPU, ROS or live calibration

## Supervisor decision

ICRA-048 repaired all three incoming defects: the real v2 launch now gives the ego-planner and both
manifests false P1/P2 metrics-only values; coordinated/isolated protocol drift rejects at an acyclic trust
anchor; and secondary v1/v2 manifests reject. Its fixed hash cascade, lineage preservation and no-live
boundary are accepted.

One High Spec gap remains. `validate_test_planner_effective_contract()` checks only the nested
`test_planner_manifest["p4.g0c"]["effective_values"]`, which is the declared protocol binding. It does not
check the production manifest's top-level effective fields generated by the actual launch resolution path.
Changing only top-level `p1.metrics_only` from false to true, keeping the nested binding/protocol false and
refreshing all inventory/state hashes, still yields `DRAFT_ELIGIBLE`, zero failures and a threshold draft.
This is the same split that hid the ICRA-047 runtime mismatch.

ICRA-049 fixes exactly this seam. It remains synthetic. A live replacement matrix may be authorized only
after a separate Supervisor PASS.

## 1. Preserve state and forbidden boundaries

- Follow `AGENTS.md` synchronization and stop on `REMOTE_DIVERGED`. Preserve all existing tracked and
  untracked user files; never reset, clean, stash, rebase, amend pushed history or overwrite another role.
- Preserve byte-for-byte ICRA-046 and its twelve build/install trees, all ICRA-047/048 compact evidence,
  v1 artifacts, v2 protocol/registry/dependency/lineage/fixture and the protected PDF.
- Put all temporary/test/review output below `results/icra27/icra049/`. Do not create a build/install tree.
- No GPU preflight, ROS, `ros2 launch`, calibration runner/analyzer CLI, live run, CTest, retained binary,
  bag/RViz, threshold draft/freeze/application action, G0C verdict, G0D, P5 or cleanup.

## 2. Bind the actual production manifest surface

- Define in the shared protocol module the exact set/mapping of every frozen protocol effective value that
  the production `test_planner_manifest.json` materializes at top level. This includes all 28 currently
  common keys, including P1/P2 metrics-only, risk-grid and P1/P2/P3/P4 switches, all seven
  `planner_enable_*` flags, manager fanout/distinctive settings, planner profile, record/RViz/validator
  flags and debug switches. Do not validate only the two values from the previous incident.
- `validate_test_planner_effective_contract()` must require each mapped top-level key to exist and compare
  it against the frozen protocol effective value with exact JSON type equality. It must continue to
  validate the complete nested G0C binding for protocol-only fields.
- The runner must reject a missing, wrong-value or Python-equal/wrong-type top-level field before marking
  the run COMPLETE or creating its final inventory. The analyzer must reject the same cases before any
  threshold draft. A caller-updated inventory/hash must not bypass semantic validation.
- Preserve current correct launch behavior and all immutable anchors. No scientific/configuration value,
  threshold formula, schema, run ID, package closure or product behavior may change.

## 3. Required parameterized evidence

- Make synthetic runner/analyzer fixtures production-shaped: every required top-level effective key must
  be present in addition to the nested `p4.g0c` binding.
- Parameterize add/change/remove/wrong-type adversaries across the complete mapped top-level key set. At
  minimum prove top-level-only P1 and P2 metrics-only drift with nested values unchanged, missing keys,
  boolean/integer substitution and numeric integer/float substitution. Refresh legitimate inventory/state
  hashes in adversaries so failures come from semantic binding, not stale provenance.
- Prove the unchanged production-shaped v2 bundle still reaches synthetic runner COMPLETE and analyzer
  `DRAFT_ELIGIBLE`, while every top-level disagreement yields no COMPLETE/no draft. Preserve all ICRA-048
  trust-anchor, scientific-contract, schema-downgrade, secondary-manifest and real-launch regressions.
- Run focused protocol/runner/analyzer/launch suites, launch golden tests, full repository Python discovery,
  Python syntax, canonical JSON, `git diff --check`, allowlist, protected-tree before/after, branch sync and
  exact zero-process audit. Set `TMPDIR=$PWD/results/icra27/icra049/tmp` for every temporary-file test.

## 4. Documentation and handoff

- Update `DEV_LOG.md`, `docs/CHANGES.md` and `docs/TRACEABILITY.md` with `IAP-RQ-423`, exact runnable
  commands/exit codes, the reproduced top-level bypass, mapped key count, and no-live/no-threshold limit.
- Stage only allowed files and compact ICRA-049 evidence. Commit/push implementation, then commit/push one
  final DEV_LOG-only handoff; every commit contains `IAP-RQ-423`.
- Report `P4_G0C_TOP_LEVEL_EVIDENCE_BINDING_READY_FOR_REVIEW`, never G0C PASS. Do not authorize or execute
  the replacement live matrix or choose another research task.

## Allowed files

- `scripts/dev_planner/p4_g0c_protocol.py`;
- `scripts/dev_planner/run_p4_g0c_calibration.py` and
  `scripts/dev_planner/analyze_p4_g0c_calibration.py` only if needed to consume the shared validator;
- focused `test/test_p4_g0c_runner.py`, `test/test_p4_g0c_analyzer.py`,
  `test/test_p4_g0c_protocol.py` and launch-contract tests only if needed to preserve the real-path proof;
- compact evidence below `results/icra27/icra049/`;
- `DEV_LOG.md`, `docs/CHANGES.md`, `docs/TRACEABILITY.md`.

## Forbidden

- No launch/config/protocol/registry/dependency/lineage/fixture, v1, ICRA-046/047/048 evidence,
  C++/header/CMake/product/scenario/geometry/P0/P1/P2/P3/P4-decision/P5 behavior or scientific-value change.
- No package build, GPU/ROS/live execution, runner/analyzer CLI, calibration, retry, threshold action,
  G0C PASS, G0D/P5/formal campaign, retained-tree execution/write, protected/external-repository change or
  build/install cleanup.
