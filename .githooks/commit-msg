#!/usr/bin/env bash
set -euo pipefail
repository=$(git rev-parse --show-toplevel)
exec python3 "$repository/scripts/dev_planner/verify_icra_research_route.py" \
  --repository "$repository" --commit-message "$1"
